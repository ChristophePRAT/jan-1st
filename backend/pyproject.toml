[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "strands-agents-tools>=0.2.19",
    "strands-agents[custom,custom-providers,openai]>=1.22.0",
]
